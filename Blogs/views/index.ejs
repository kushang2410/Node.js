<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <link rel="icon" href="../public/img/Netflix_icon.svg.png" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container-fluid">
        <%- include('./partials/header.ejs') %>
            <div class="row">
                <%- include('./partials/aside.ejs') %>
                    <div class="col-md-10">
                        <div class="row ms-5">
                            <% blogs.forEach(blog=> { %>
                                <div class="col-md-11 mb-3">
                                    <div class="card mb-3 border-0">
                                        <img src="<%= blog.Image %>" class="card-img-top" alt="blog Image" height="auto">
                                        <div class="card-body">
                                            <h5 class="card-title fw-bold fs-2 mb-4">
                                                <%= blog.Name %>
                                            </h5>
                                            <p class="card-text mb-3">
                                                <%= blog.Description %>
                                            </p>
                                            <a href="/edit/<%= blog._id %>" class="btn btn-outline-dark">Edit</a>
                                            <form action="/delete/<%= blog._id %>" method="post" class="d-inline">
                                                <button type="submit" class="btn btn-outline-dark">Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                                <% }) %>
                        </div>
                    </div>
            </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>